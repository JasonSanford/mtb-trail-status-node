extends base

block content
  .jumbotron
    h1 Get Text Alerts
    h2 for charlotte area trail statuses
    p
      | Be the first to know when trails open or close. We constantly monitor trail status updates from  
      a(href='http://www.tarheeltrailblazers.com/') Tarheel Trailblazers 
      | and other sources and text you as soon as your favorite trail opens or closes.
  h2 Charlotte Area Mountain Bike Trail Statuses
  table.table.table-striped
    tbody
      each trail in trails
        tr.trail
          td
            h3
              - glyphicon = trail.status === "open" ? 'glyphicon-ok' : 'glyphicon-remove'
              i.glyphicon(class='#{glyphicon} #{trail.status}')
              | 
              =trail.name
          td.date.text-muted
            - localDate = trail.status_date.toLocaleDateString()
            - localTime = trail.status_date.toLocaleTimeString()
            - timeParts = localTime.split(' ')
            - hourParts = timeParts[0].split(':')
            - time      = hourParts.splice(0, 2).join(':')
            span(class='#{trail.status}')
              =trail.status
            | 
            |as of 
            =localDate
            |, 
            =time
            | 
            =timeParts[1].toLowerCase()